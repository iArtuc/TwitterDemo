<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="android:windowBackground">@color/White</item>

        <item name="colorControlNormal">@color/Orange</item>
        <item name="colorControlActivated">@color/BoldGray</item>
        <item name="colorControlHighlight">@color/OrangeRed</item>
        <item name="android:textColorHint">@color/BoldGray</item>
        <item name="android:textColor">@color/TextGray</item>

        <item name="colorButtonNormal">@color/OrangeRed</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.Light">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>
